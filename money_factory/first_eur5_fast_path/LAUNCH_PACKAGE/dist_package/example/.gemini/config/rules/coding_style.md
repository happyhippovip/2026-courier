# Coding Style & Security Guidelines

- Always use async/await rather than raw promise chains.
- Never log plaintext secrets, passwords, or private API keys to console.
- Validate all user input schema before database insertion.
