[pytest]
# Root-level test_*.py files are manual smoke scripts with import-time side
# effects (live localhost calls, agent launches); never collect them.
testpaths = tests
norecursedirs = tests/acceptance tests/boundaries
pythonpath = .
