# WALL-P2-PILOT-CONVERSION — demo-to-pilot + followup (MAC-04)

TASK_ID=WALL-P2-PILOT-CONVERSION · STATUS=DONE · WORKER=MAC-04 · HOST=MAC ·
MODE=READ_ONLY · 2026-09-26T15:52Z. Follows WALL-P2-DEMO-STORYBOARD-MAC02
(demo) and WALL-P2-PILOT-SIGNUP-MAC-21 (signup); covers only the handoff
between them plus post-pilot followup.

## DEMO_TO_PILOT_CONVERSION_FLOW
1. End every demo with the SAME question: "Want this running on YOUR folder
   for two weeks?" (no pitch deck, no pricing yet).
2. YES -> run the 3-step signup immediately (same session if possible):
   scope handshake -> witness test -> first scoped run. Price variant is
   chosen AFTER the witness test passes, never before.
3. Witness test FAILS -> stop. Say: "This failed in front of you -- that is
   the system telling the truth. I will come back when this check passes."
   Log the failure; do not discount, do not workaround live.
4. MAYBE -> leave the proof card + one-page offer; one followup (below);
   then silence. No drip sequence in pilot phase.

## FOLLOWUP_MESSAGE (send once, 2 days after demo-or-pilot-end)
"Hi <name> -- your <chore> ran <N> days out of <M>, with <K> times I had to
step in after the start (<card refs>). <Verdict: met / did not meet> the
success list we wrote down. Want a second two weeks <same terms / adjusted
scope: ...>? Either answer is fine -- your proof cards and state export are
attached regardless."
Rules: real numbers from cards only; no new claims; attach the export;
no second followup unless they reply.
