# WALL-P2-DEMO-STORYBOARD — evidence-backed 60s storyboard (PREP ONLY, no recording)

WORKER=MAC-02 (MUSE) · HOST=MAC · MODE=READ_ONLY · 2026-09-26
PREREQUISITE Z12 demo scripts: NOT FOUND in bounded search
(burn30/evening/wall/muse_45/muse_30/koordination) → UNKNOWN stays UNKNOWN.
Built instead on MM7_REAL_DEMO_STORYBOARD (master script) + GM5 canary proof
(already-aired run incl. kill -9). No invented results. No repo writes.

## Format
Split screen — LEFT: state card (6 grandma states project). RIGHT: terminal
evidence. Total 60s. Voiceover DE, captions DE/EN. Each scene tagged:
[PROVEN] = already observed in GM5 canary run · [SCRIPTED] = per MM7 master,
needs one fresh recorded run · [PLACEHOLDER] = proof missing.

## Scenes
00:00–00:07 AUFTRAG rein [SCRIPTED] — POST /goals with workflow_plan [A,B];
state QUEUED. MM7 Scene 1 exact curl reusable. Caption: "Ein Auftrag, zwei
Schritte. Ab jetzt keine Tastatur mehr."
00:07–00:15 A arbeitet [PROVEN pattern / SCRIPTED instance] — claim →
DISPATCHED → isolated execution, exit 0. GM5 logged this shape; fresh run
re-records PIDs/hashes.
00:15–00:23 A fertig [PROVEN pattern] — DurableResult + sha256 → RESULT_RECEIVED.
00:23–00:32 A geprüft [PROVEN] — independent verifier byte-hash MATCH → PASS →
RECONCILED, step index 0→1. On-screen caveat (mandatory, 3s overlay):
"Prüfung = exakter Hash-Abgleich bei Testaufgaben, kein allgemeines Urteil."
(GM5 clarification; keeps RV19/GAP2 honest inside the demo.)
00:32–00:40 B startet allein [PROVEN] — zero human requests between reconcile
and B-claim (server.log grep, GM5 method). Caption: "Kein Klick. Kein Anruf."
00:40–00:48 B fertig [PROVEN pattern] — result + PASS → goal DONE/COMPLETED.
00:48–00:55 Relay-Zähler [PROVEN method] — audit-trail scan, HUMAN_RELAYS=0
(GM5 §HUMAN_RELAY_COUNT recipe). This is the demo's one number; it is counted,
not estimated (grandma no-fake-metrics rule).
00:55–01:00 Proof card [SCRIPTED] — goal id, both artifact SHAs, attempt chain,
verifier independence (distinct key/PID), candidate SHA. Feeds RV08 card spec.

## Placeholders (proof missing — show as "next" slate, never as footage)
- P1: restart DURING the recorded run (GM5 proved kill-9 after A-reconcile on
  the earlier run; the new recording should include it only if
  WALL-P1-RESTART-MATRIX defines the exact kill point — do not improvise).
- P2: any customer-shaped (non-fixture) task — forbidden until semantic
  verification exists (RV19/GAP2). Demo stays fixture-scoped, labeled on screen.
- P3: Z12 scripts if they surface — if identical beats exist there, dedupe to
  a pointer (E24 rule); UNKNOWN until found.

## Reuse (no duplicate work)
Beats/scenes: MM7 master. Proof: GM5. Card fields: RV08. Metrics honesty:
grandma surface. Oma/Mama/X voiceovers: MM7 §2 (adopt, don't rewrite).
Z14 pilot material: referenced by WALL_STATUS for PILOT-SIGNUP, out of scope here.

STATUS=STORYBOARD_COMPLETE · recording NOT started (needs GO-gated canary slot,
owned by Mac physical-proof runner — no second canary from this worker)
